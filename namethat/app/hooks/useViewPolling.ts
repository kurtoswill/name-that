import { useState, useEffect } from 'react';

// Polling removed. This hook is now a no-op. Use usePostView in PostCard for engagement-based view updates.
export function useViewPolling() {
    return {};
}
